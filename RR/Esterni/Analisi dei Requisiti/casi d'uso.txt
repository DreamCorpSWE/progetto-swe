Utente
-Inserire rete bayesiana in formato json (Grafana)
 -upload del file intero
  -file corrotto
  -file giusto
 -raw paste del file
  -errore sintattico segnalato dal parser
  -file giusto
 -upload corretto

-Associare/modificare i nodi della rete con i dati di grafana (Grafana)
 -Selezionare un pannello
  -Collegare il valore del pannello ad uno dei nodi
  -Errore: flusso dati collegato ad un altro nodo
   -Scollega il vecchio nodo e collega il nuovo
   -Cambiare lo stato del nodo in base al valore ricevuto
  -Se il pannello è un grafo temporale:
   -Lo stato del nodo cambia in base al valore soglia del dato

Grafana (software)
-Lanciare un alert in base alle soglie dei nodi (Utente)
-Visualizzazione risultati ottenuti dall'output della rete (Utente)
-Ricalcolo probabilità della rete (jsbayes)

jsbayes (software)
-Calcolare gli output della rete in base ai dati ricevuti in ingresso (jsbayes)
-Fornire i dati ottenuti al sistema Grafana (Grafana)

